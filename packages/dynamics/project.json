{
  "name": "dynamics",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/dynamics/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist && bun run ../../scripts/build.ts --external @ue-too/math",
        "cwd": "packages/dynamics"
      }
    },
    "build:bun": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist && bun run ../../scripts/build.ts --external @ue-too/math",
        "cwd": "packages/dynamics"
      }
    },
    "move-package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node ../../scripts/move-package.mjs",
        "cwd": "packages/dynamics"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test",
        "cwd": "packages/dynamics"
      }
    },
    "nx-release-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish --access public --registry https://registry.npmjs.org --no-git-checks $( [ \"{args.dryRun}\" = \"true\" ] && echo \"--dry-run\" )",
        "forwardAllArgs": false,
        "cwd": "packages/dynamics/dist"
      }
    },
    "docs:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf docs/dynamics && bun run typedoc --entryPoints packages/dynamics/src/index.ts --out docs/dynamics"
      }
    }
  },
  "tags": [],
  "implicitDependencies": [
    "math"
  ]
}
