import './App.css';

/**
 * Main React App component.
 * @returns Root React component
 */
const App = (): React.ReactNode => {
    return (
        <div className="app">
            <h1>Knit</h1>
        </div>
    );
};

export default App;
