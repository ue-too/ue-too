{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "implicitDependencies": [
        "math",
        "being",
        "animate",
        "border",
        "curve",
        "dynamics",
        "board"
    ],
    "name": "banana",
    "projectType": "application",
    "sourceRoot": "apps/banana/src",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun run build",
                "cwd": "apps/banana"
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun test",
                "cwd": "apps/banana"
            }
        }
    }
}
