{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "implicitDependencies": ["dynamics", "math", "board"],
    "name": "horse-racing",
    "projectType": "application",
    "sourceRoot": "apps/horse-racing/src",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun run build",
                "cwd": "apps/horse-racing"
            }
        },
        "dev": {
            "executor": "nx:run-commands",
            "options": {
                "command": "vite",
                "cwd": "apps/horse-racing"
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun test",
                "cwd": "apps/horse-racing"
            }
        }
    }
}
